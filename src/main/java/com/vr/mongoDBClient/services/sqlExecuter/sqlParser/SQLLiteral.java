package com.vr.mongoDBClient.services.sqlExecuter.sqlParser;

public enum SQLLiteral {
    SELECT,
    FROM,
    WHERE,
    GROUPBY,
    ORDERBY,
    SKIP,
    LIMIT,
    
    AND,
    OR,    
    EQ,
    GT,
    GTE,    
    LT,
    LTE,
    
    ASC,
    DESC
}
