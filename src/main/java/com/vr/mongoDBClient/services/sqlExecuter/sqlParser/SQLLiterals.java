package com.vr.mongoDBClient.services.sqlExecuter.sqlParser;

public enum SQLLiterals {
    SELECT,
    FROM,
    WHERE,
    GROUPBY,
    ORDERBY,
    ASC,
    DESC,
    SKIP,
    LIMIT,
    
    AND,
    OR,
    
    GT,
    GTE,    
    LT,
    LTE
}
